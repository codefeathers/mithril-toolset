import { InputOptions, OutputOptions } from "rollup";
export declare function build({ inputOptions, outputOptions, }: {
    inputOptions: InputOptions;
    outputOptions: OutputOptions;
}): Promise<void>;
